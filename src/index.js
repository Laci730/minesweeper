import React from 'react';
import ReactDOM from 'react-dom/client';
import './root.css';
import Field from './field-components/field';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <Field />
);
